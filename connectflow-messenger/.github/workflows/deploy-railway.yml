name: Deploy to Railway

on:
  push:
    branches: [main]
  workflow_dispatch:
  repository_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to Railway
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to Railway
        uses: amondnet/railway-deployment@v25
        with:
          railway_token: ${{ secrets.RAILWAY_TOKEN }}
          railway_service: ${{ secrets.RAILWAY_SERVICE }}
          environment: production
          args: up
          
      - name: Get Service URL
        id: service
        run: |
          echo "DEPLOY_URL=${{ secrets.RAILWAY_SERVICE }}.up.railway.app" >> $GITHUB_OUTPUT
          
      - name: Show Deployment URL
        run: |
          echo "🎉 Application deployed successfully!"
          echo "URL: https://${{ steps.service.outputs.DEPLOY_URL }}"
