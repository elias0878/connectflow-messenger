services:
  - type: web
    name: connectflow-messenger
    env: node
    nodeVersion: 20
    buildCommand: npm install && npm rebuild better-sqlite3
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
